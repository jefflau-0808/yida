# 默认 mock 不会调用外部服务；real 会在服务端调用 OpenAI。
YIDA_AI_MODE=mock
YIDA_SESSION_SECRET=replace-with-a-long-random-production-secret

# 仅在 YIDA_AI_MODE=real 时需要。不要使用 NEXT_PUBLIC_ 前缀，也不要提交真实密钥。
OPENAI_API_KEY=
YIDA_OPENAI_TEXT_MODEL=gpt-5.6-luna
YIDA_OPENAI_TIMEOUT_MS=30000
